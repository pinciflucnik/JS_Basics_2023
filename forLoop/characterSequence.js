function characterSequence(arg){
    let text = arg[0];
     for (i = 0; i < text.length; i++){
         let letter = text[i];
         console.log(letter);
         
    }

}
characterSequence(["do"])