function validOrInvalid(arg){

    let number = Number(arg[0]);

    if (number != 0 && number < 100 || number > 200){
        console.log("invalid");
    }
    else {
        
    }
}
validOrInvalid(["201"])